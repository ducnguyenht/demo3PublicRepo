import 'package:flutter/material.dart';

class PageSearchResult extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
        appBar: new AppBar(title: new Text('Kết quả tìm kiếm')),
        body: new Text('Search result page')
    );
  }
}
