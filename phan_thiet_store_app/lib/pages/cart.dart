import 'package:flutter/material.dart';

class PageCart extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(title: new Text('Giỏ hàng')),
      body: new Text('Cart page')
    );
  }
}
